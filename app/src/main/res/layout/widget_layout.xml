<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/transparent">

    <!-- Top Section with Profile Picture and Name -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:background="@color/white"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- Left Navigation Button -->
        <ImageButton
            android:id="@+id/btn_previous_friend"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_media_previous"
            android:contentDescription="Previous Friend"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            app:tint="@color/orange_primary" />

        <!-- Profile Picture -->
        <ImageView
            android:id="@+id/img_profile"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/img"
            android:background="@color/light_gray"
            android:scaleType="centerCrop"
            android:contentDescription="Friend Profile Picture" />

        <!-- Friend Name and Index -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp">

            <!-- Friend Name -->
            <TextView
                android:id="@+id/txt_friend_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Login"
                android:textSize="20sp"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Friend Index (e.g., "1 of 3") -->
            <TextView
                android:id="@+id/txt_friend_index"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="12sp"
                android:textColor="@color/text_gray"
                android:layout_marginTop="2dp" />
        </LinearLayout>

        <!-- Right Navigation Button -->
        <ImageButton
            android:id="@+id/btn_next_friend"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@android:drawable/ic_media_next"
            android:contentDescription="Next Friend"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:layout_marginEnd="8dp"
            app:tint="@color/orange_primary" />

    </LinearLayout>

    <!-- Bottom Section with Orange Background and Mic Button -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:background="@color/orange_primary"
        android:gravity="center">

        <!-- Microphone Button -->
        <ImageView
            android:id="@+id/btn_start_recording"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_microphone"
            android:contentDescription="Record and Send Audio"
            android:padding="12dp"
            app:tint="@color/dark_gray"
            android:background="?android:attr/selectableItemBackgroundBorderless" />
    </RelativeLayout>

</LinearLayout>
